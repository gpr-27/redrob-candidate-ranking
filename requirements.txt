# No third-party dependencies.
# The entire ranking pipeline uses only the Python 3 standard library
# (json, csv, argparse, datetime, re, pathlib, time).
#
# Tested with Python 3.11+ (developed on 3.13).
