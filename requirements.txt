# Core Ranking Pipeline: Zero external dependencies (uses Python standard library only).
# The dependencies below are required ONLY for the Streamlit interactive sandbox UI and the PPTX deck generator.

streamlit>=1.35.0
pandas>=2.0.0
openpyxl>=3.1.0
python-pptx>=0.6.21
